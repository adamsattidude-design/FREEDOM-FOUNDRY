import { readJson } from '../lib/store';import { bot } from '../lib/types';
export default async function FAQ(){const content=await readJson<any>('content.json',{});return <main className="wrap"><span className="badge">KB-backed FAQs</span><h1 className="sectionTitle">Freedom Foundry FAQs</h1><p className="lead">Botpress KB: {bot.kbId} • File: {bot.kbFile}</p><div className="grid2">{(content.faqs||[]).map((f:any)=><details className="glass panel" key={f.id} open><summary><b>{f.question}</b></summary><p className="muted">{f.answer}</p><span className="pill">{f.category}</span></details>)}<details className="glass panel"><summary><b>Does ATTITUDE replace Gideon?</b></summary><p className="muted">No. ATTITUDE helps with qualification, support and systems. Protected decisions like payments, refunds and access still need approval.</p></details></div></main>}
