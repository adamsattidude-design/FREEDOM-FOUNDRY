"use client";import { useState } from 'react';
export default function QuoteForm(){const[done,setDone]=useState('');async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const body=Object.fromEntries(f.entries());for(const k of ['name','businessName','phone','email','serviceNeeded','budget','description']){if(!String((body as any)[k]||'').trim()){alert('Please fill '+k);return}}const r=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});setDone(r.ok?'Saved. Gideon will review this lead.':'Could not save lead');e.currentTarget.reset()}return <div className="glass panel"><h2>Request a Quote</h2><form onSubmit={submit} className="form"><input name="name" className="input" placeholder="Name" required/><input name="businessName" className="input" placeholder="Business name" required/><input name="phone" className="input" placeholder="Phone" required/><input name="email" className="input" type="email" placeholder="Email" required/><select name="serviceNeeded" required><option value="">Service needed</option><option>Website</option><option>Full-stack platform</option><option>Student portal</option><option>Landing page / funnel</option><option>Automation / ATTITUDE bot</option></select><input name="budget" className="input" placeholder="Budget e.g. 3000 GHS" required/><textarea name="description" placeholder="Describe what you need" required/><button className="btn goldbtn">Send Quote Request</button>{done&&<b className="gold">{done}</b>}</form></div>}
