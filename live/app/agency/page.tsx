import { readJson } from '../lib/store';import { Service } from '../lib/types';import QuoteForm from '../components/QuoteForm';
export default async function Agency(){const services=await readJson<Service[]>('services.json',[]);return <main className="wrap"><span className="badge">Freedom Foundry Agency</span><h1 className="sectionTitle">High-ticket websites, platforms, funnels and automation.</h1><p className="lead">No Selar link here. Request a quote for business builds: websites, full-stack platforms, student portals, dashboards, booking systems, landing pages, sales funnels and ATTITUDE automation.</p><div className="grid">{services.map(s=><article className="glass card" key={s.id}><img src={s.portfolioImages?.[0]}/><div className="cardbody"><h2>{s.title}</h2><p className="muted">{s.description}</p><b className="price">From {s.startingPrice} GHS</b><ul className="list">{s.features.map(f=><li key={f}>{f}</li>)}</ul></div></article>)}</div><section className="grid2" style={{marginTop:28}}><div className="glass panel"><h2>What ATTITUDE can do</h2><ul className="list"><li>Build websites, landing pages, portals, apps and dashboards</li><li>Write and debug code, create APIs, manage databases</li><li>Manage servers, VPS, deployments and automation jobs</li><li>Connect Botpress, n8n, CRM, email, WhatsApp and reports</li><li>Capture leads, qualify buyers and escalate protected issues</li></ul></div><QuoteForm /></section></main>}
